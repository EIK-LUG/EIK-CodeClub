
1) Return the opposite bool

    bool opposite(bool b)

    ex. opposite(True) -> False

2) Is divisible by

    bool isDivisibleBy(int value, int divBy)

    ex. isDivisible(4, 2) -> True

3) Sum elements in array (don't use built-ins :) )

    int sum(int[])

    ex. sum({1, 2, 3, 4}) -> 10

4) Take a number and return the sum of it's digits

    int sumDigits(int number)

    ex. sumDigits(1234) -> 10

5) Take a number and return a 4 character string that contains whole number digits other than 0 that summed up are the input

    string reverseSumDigits(int number)

    ex. reverseSumDigits(10) -> "4411"